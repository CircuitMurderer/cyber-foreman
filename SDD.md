# 赛博监工：Spec-Driven Development 规约

> 版本：0.1  
> 状态：试行  
> 适用范围：Cyber Foreman 的功能、协议、恢复策略和安全策略开发

## 1. 我们所说的 SDD

本项目中的 SDD 指 **Spec-Driven Development（规格驱动开发）**：先把目标写成可验证的行为契约，再进行设计、实现和验收。

规格不是需求说明的附件，而是开发过程的主线：

```text
意图 → 规格 → 设计 → 任务 → 实现 → 证据 → 验收
```

对于赛博监工，SDD 尤其重要。被监督的 Agent、监督模型以及外部 CLI 都可能产生不稳定输出，因此系统是否正确不能由某个模型声称“已经完成”来决定，必须由明确状态、结构化事件和可复现证据决定。

## 2. 核心原则

### 2.1 规格先于实现

新增行为必须先有规格。实现过程中发现规格不成立时，先修订规格，再调整代码。

### 2.2 描述行为，不预设代码

规格说明系统对外表现、约束和验收方式。目录、类型和算法属于设计，除非它们本身是兼容性要求。

### 2.3 每个要求都必须可验证

避免“智能地判断”“可靠地恢复”“尽可能安全”一类无法验收的描述。要求应说明输入、条件、期望结果和失败结果。

### 2.4 确定性事实优先于模型判断

进程退出码、测试结果、Git diff、权限规则和状态迁移是事实。LLM 用来处理模糊判断，不能覆盖确定性安全规则。

### 2.5 自治必须有边界

所有重试、纠偏和恢复都必须有次数、时间、资源与权限边界。超过边界后进入明确的人工处理状态。

### 2.6 完成必须有证据

Agent 的自然语言回答不能作为完成依据。任务必须通过规格声明的验证器和完成门禁。

## 3. 规格产物

每个独立能力使用一个目录：

```text
specs/
  001-grok-acp-adapter/
    spec.md       # 需求与验收契约
    design.md     # 技术设计和关键取舍
    tasks.md      # 可执行任务与依赖关系
```

必要时增加：

```text
    fixtures/     # 协议样例和脱敏事件
    decisions/    # 仅影响此能力的设计决策
```

跨功能、长期有效的架构决策放在：

```text
docs/decisions/ADR-NNN-title.md
```

产物职责：

| 文件 | 回答的问题 | 不应包含 |
|---|---|---|
| `spec.md` | 系统必须表现成什么样 | 大段具体实现代码 |
| `design.md` | 准备如何实现以及为何这样实现 | 尚未确认的产品要求 |
| `tasks.md` | 以什么顺序完成和验证 | 无法对应规格的杂项工作 |

## 4. 生命周期

规格状态使用以下值：

```text
DRAFT → READY → IMPLEMENTING → VERIFYING → ACCEPTED
                  └──────────────→ BLOCKED
ACCEPTED → SUPERSEDED
```

- `DRAFT`：允许存在开放问题，不允许开始正式实现。
- `READY`：范围、接口、异常行为和验收标准已经明确。
- `IMPLEMENTING`：实现进行中；行为变更必须同步修改规格。
- `VERIFYING`：实现完成，正在收集验收证据。
- `ACCEPTED`：所有必需验收项通过，没有未处理的高风险问题。
- `BLOCKED`：存在无法在当前权限或上下文内解决的阻塞。
- `SUPERSEDED`：已被新规格明确替代。

## 5. `spec.md` 模板

```markdown
# SPEC-NNN：能力名称

- 状态：DRAFT
- 负责人：
- 创建日期：YYYY-MM-DD
- 最后更新：YYYY-MM-DD
- 关联 issue / ADR：

## 背景与问题

当前发生了什么，为什么值得解决。

## 目标

- 可观察、可验证的结果。

## 非目标

- 本次明确不处理的事项。

## 使用场景

### SCN-001：场景名称

Given 前置条件  
When 发生动作  
Then 可观察结果

## 功能要求

- REQ-001：系统必须……
- REQ-002：当……时，系统必须……

## 状态与不变量

- INV-001：任何时候都必须成立的约束。

## 接口与事件

- 输入：
- 输出：
- 错误：
- 兼容性：

## 失败与恢复

- 可重试失败：
- 不可重试失败：
- 重试预算：
- 人工升级条件：

## 安全与权限

- 文件、命令、网络和凭据边界。

## 可观测性

- 必须产生的事件、日志、指标和审计字段。

## 验收标准

- AC-001：……
- AC-002：……

## 验证矩阵

| 要求 | 验收项 | 测试/命令 | 证据 |
|---|---|---|---|
| REQ-001 | AC-001 | `./scripts/go test ...` | 待生成 |

## 开放问题

- Q-001：……
```

## 6. 要求的写法

使用以下强度词：

- **必须**：不满足则规格失败。
- **应该**：默认满足；偏离时必须记录理由。
- **可以**：可选能力，不作为完成门禁。

一条要求只表达一个主要行为，并分配稳定编号。修改措辞时保留编号；语义完全改变时创建新编号并废弃旧编号。

推荐形式：

```text
当 <条件> 时，系统必须 <可观察行为>，并在 <边界> 内产生 <结果/事件>。
```

示例：

```text
REQ-007：当 Adapter 子进程非正常退出时，系统必须记录退出码，
将任务从 running 转为 recovering 或 failed，并产生 agent.exited 事件。
```

## 7. 赛博监工的不变量

除非后续规格明确替代，以下约束始终成立：

- `INV-001`：任务只能沿 `internal/task` 声明的合法路径迁移状态。
- `INV-002`：Agent 的完成声明不能直接把任务置为 `completed`。
- `INV-003`：安全拒绝的优先级高于模型建议、自动批准和恢复动作。
- `INV-004`：每次自动重试和纠偏都消耗预算；预算耗尽后必须停止自动执行。
- `INV-005`：Adapter 必须声明能力，控制层不能假设所有 Agent 都支持恢复、中途消息或结构化工具事件。
- `INV-006`：优先使用 ACP、JSON-RPC、JSONL 或 Hook；终端文本解析只能作为降级方案。
- `INV-007`：任务完成必须关联可重放的验收证据。
- `INV-008`：系统必须允许完全使用内网 OpenAI-compatible 模型端点。
- `INV-009`：未启用认证时，控制面只能默认监听回环地址。
- `INV-010`：日志与事件不得记录未脱敏的凭据。

## 8. `design.md` 必答项

设计文档至少回答：

1. 数据从哪里进入，经过哪些组件，最终存到哪里？
2. 哪些判断是确定性规则，哪些判断交给本地模型？
3. 进程、服务或连接中断后，从什么检查点恢复？
4. 幂等边界在哪里？同一个事件重复到达会怎样？
5. 并发任务是否共享工作目录、Git 分支或凭据？
6. 如何取消任务并清理子进程树？
7. 哪些操作需要人工批准？
8. 如何在没有外网的环境安装、运行和诊断？
9. 如何回滚实现或关闭新能力？

存在以下情况时应编写 ADR：

- 引入新的外部依赖或持久化组件；
- 修改公开 API、事件 schema 或 Adapter 接口；
- 改变安全边界、默认权限或网络暴露范围；
- 选择一种难以逆转的协议或存储格式。

## 9. `tasks.md` 规则

任务必须小到可以独立验证，并引用对应要求：

```markdown
- [ ] T-001 [REQ-001, REQ-002] 定义 ACP 消息类型和编码测试
- [ ] T-002 [REQ-003] 实现 stdio JSON-RPC 传输
- [ ] T-003 [REQ-004, AC-002] 增加进程断开恢复测试
- [ ] T-004 [AC-001..AC-004] 执行验收并记录证据
```

如果一个实现任务无法关联任何要求，应删除该任务或先补充规格。

## 10. 验证与追踪

验证分为四层：

1. **单元验证**：状态迁移、协议编码、规则判断。
2. **组件验证**：Adapter 与模拟 Agent 进程交互。
3. **故障验证**：超时、断联、重复事件、输出阻塞、恢复失败。
4. **验收验证**：从用户提交任务到完成门禁的完整路径。

最低交付检查：

```bash
./scripts/go fmt ./...
./scripts/go vet ./...
./scripts/test
```

测试通过只是证据之一。涉及状态、协议或安全的功能还必须提供对应的事件样例或端到端结果。

## 11. 确定性监督闭环

Rule-based Supervisor 必须采用以下单向数据流：

```text
Agent / Timer / Git / Test
            │
            ▼
       Observation
            │
            ▼
 Task Snapshot + Rule Engine
            │
            ▼
         Decision
            │
            ▼
      Action Executor
            │
            ▼
          Adapter
```

各层职责不得混合：

- **Probe** 只采集事实，例如有效进展时间、退出码、Git 增量和测试结果。
- **Rule Engine** 必须是无副作用的确定性计算，只能输出结构化 Decision。
- **Action Executor** 是唯一允许调用 Adapter、验证命令和状态迁移的组件。
- **Task Snapshot** 保存规则需要的当前状态、预算和最近动作，不保存 API Key 等凭据。

每个 Decision 必须包含稳定的 `rule_id`、动作、理由、证据引用和去重键。重复 Observation 不得重复消耗预算或执行动作。所有自动纠偏必须受次数、时间和权限预算约束。

完成判定采用门禁而不是 Agent 自述：

```text
Agent end_turn / process exit
            │
            ▼
        verifying
       ┌────┴────┐
       ▼         ▼
     tests      git
       └────┬────┘
            ▼
 completed / corrective action / attention_required
```

本地 LLM 将来只能提供建议型 Decision；确定性安全拒绝、测试失败、Git 越界和预算耗尽拥有更高优先级。

## 12. 变更纪律

- 实现与规格冲突时，以已批准规格为准。
- 需求变化时，在同一次变更中更新规格、设计、任务和测试。
- 修复缺陷时，先添加能够复现问题的验收项或测试。
- 不允许为了让测试通过而弱化未修改的验收标准。
- 对外接口变更必须说明兼容策略和迁移路径。
- 已接受规格发生实质变化时，应恢复到 `DRAFT` 或创建替代规格。

## 13. Definition of Ready

规格进入 `READY` 前必须满足：

- 目标和非目标明确；
- 主要场景、失败路径和恢复边界明确；
- 每条强制要求可以被测试或检查；
- 权限、数据和网络边界已说明；
- 没有会实质改变方案的开放问题；
- 已确定完成门禁和验证命令。

## 14. Definition of Done

功能进入 `ACCEPTED` 前必须满足：

- 所有“必须”要求已实现；
- 验证矩阵不存在空白的强制项；
- 格式化、静态检查和测试通过；
- 故障与恢复路径经过验证；
- 文档和实际接口一致；
- 没有未披露的安全边界变化；
- 验收证据能够关联到具体代码版本。

## 15. 下一项建议规格

下一项功能建议建立：

```text
specs/003-deterministic-supervision-loop/
```

范围包含：把 Adapter 事件接入应用层监督循环，建立 Observation、Decision、Action 和预算模型，实现 timeout、Git 与 test 的确定性规则及完成门禁。本地模型判断、SQLite 持久化和多机调度分别放入后续规格。
